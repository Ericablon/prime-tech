import { Bell, Menu, Search, X } from 'lucide-react';
import { useState, type ReactNode } from 'react';
import { useAuth } from '../../contexts/AuthContext';
import { Sidebar } from './Sidebar';
export function AppShell({children}:{children:ReactNode}){const {user,logout}=useAuth();const [mobile,setMobile]=useState(false);return <div className="app-shell"><div className="desktop-sidebar"><Sidebar/></div>{mobile&&<><div className="mobile-overlay" onClick={()=>setMobile(false)}/><div className="mobile-drawer"><button className="drawer-close" onClick={()=>setMobile(false)}><X/></button><Sidebar mobile onNavigate={()=>setMobile(false)}/></div></>}<div className="app-main"><header className="topbar"><button className="menu-button" onClick={()=>setMobile(true)}><Menu/></button><div className="search"><Search size={17}/><input placeholder="Buscar OS, cliente ou equipamento..."/></div><div className="top-actions"><button className="icon-button"><Bell size={18}/><span className="dot"/></button><div className="user-menu"><div className="avatar">{user?.full_name?.[0]??'U'}</div><div><strong>{user?.full_name}</strong><small>{user?.role_code}</small></div><button onClick={()=>void logout()} className="text-button">Sair</button></div></div></header><main className="content">{children}</main></div></div>}
