import { Filter, Plus, Search } from 'lucide-react';import { Link } from 'react-router-dom';import { PageHeader } from '../components/ui/PageHeader';import { StatusBadge } from '../components/ui/StatusBadge';import { demoOrders } from '../data/demoSeed';
export function OrdersPage(){return <><PageHeader eyebrow="Operação" title="Ordens de Serviço" description="Acompanhe o ciclo completo: entrada, diagnóstico, orçamento, execução, qualidade e entrega." actions={<Link className="primary-button" to="/ordens/nova"><Plus size={17}/>Nova OS</Link>}/><section className="panel"><div className="filters"><div className="filter-search"><Search size={16}/><input placeholder="Buscar por OS, cliente ou equipamento"/></div><button className="ghost-button"><Filter size={16}/>Filtros</button></div><div className="table-wrap"><table><thead><tr><th>OS</th><th>Cliente</th><th>Equipamento</th><th>Técnico</th><th>Status</th><th>Prioridade</th></tr></thead><tbody>{demoOrders.map(o=><tr key={o.id}><td><Link to={`/ordens/${o.id}`}><strong>#{o.order_number}</strong></Link></td><td>{o.client_name}</td><td>{o.equipment}</td><td>{o.technician??'Não definido'}</td><td><StatusBadge status={o.status}/></td><td><span className={`priority priority-${o.priority}`}>{o.priority}</span></td></tr>)}</tbody></table></div></section></>}
