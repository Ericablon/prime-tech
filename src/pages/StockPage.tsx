import { AlertTriangle, Boxes, PackageCheck, PackageMinus, PackagePlus } from 'lucide-react';import { MetricCard } from '../components/ui/MetricCard';import { PageHeader } from '../components/ui/PageHeader';import { demoStock } from '../data/demoSeed';
export function StockPage(){const critical=demoStock.filter(i=>i.physical-i.reserved<=i.minimum).length;return <><PageHeader eyebrow="Estoque" title="Peças e materiais" description="Saldo físico, reservado e disponível. Nenhuma baixa acontece sem movimentação rastreável." actions={<button className="primary-button"><PackagePlus/>Nova entrada</button>}/><div className="metrics-grid"><MetricCard label="Itens cadastrados" value={demoStock.length} icon={Boxes}/><MetricCard label="Reservas ativas" value={demoStock.reduce((s,i)=>s+i.reserved,0)} icon={PackageCheck} tone="violet"/><MetricCard label="Estoque crítico" value={critical} icon={AlertTriangle} tone="amber"/><MetricCard label="Movimentações hoje" value="18" icon={PackageMinus} tone="green"/></div><section className="panel"><div className="table-wrap"><table><thead><tr><th>SKU / item</th><th>Físico</th><th>Reservado</th><th>Disponível</th><th>Mínimo</th><th>Situação</th></tr></thead><tbody>{demoStock.map(i=>{const available=i.physical-i.reserved;const isCritical=available<=i.minimum;return <tr key={i.id}><td><strong>{i.name}</strong><small>{i.sku}</small></td><td>{i.physical}</td><td>{i.reserved}</td><td><strong>{available}</strong></td><td>{i.minimum}</td><td><span className={`stock-state ${isCritical?'critical':'ok'}`}>{isCritical?'Repor estoque':'Normal'}</span></td></tr>})}</tbody></table></div></section></>}
