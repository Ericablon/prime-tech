import { Eye, EyeOff, ShieldCheck } from 'lucide-react';
import { useState } from 'react';
import { Navigate } from 'react-router-dom';
import { useAuth } from '../contexts/AuthContext';
import type { RoleCode } from '../types/domain';
const roles:Array<{role:RoleCode;label:string}>=[{role:'gestor',label:'Gestor'},{role:'tecnico',label:'Técnico'},{role:'comercial',label:'Comercial'},{role:'atendimento',label:'Atendimento'}];
export function LoginPage(){const {user,mode,loginDemo,loginWithPassword}=useAuth();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [show,setShow]=useState(false);const [error,setError]=useState('');const [busy,setBusy]=useState(false);if(user)return <Navigate to="/" replace/>;return <main className="login-page"><div className="login-glow login-glow-a"/><div className="login-glow login-glow-b"/><section className="login-card"><div className="login-brand"><div className="login-logo">P</div><span>CRONOS</span><small>Gestão de Assistência Técnica</small></div>{mode==='demo'?<><p className="demo-label">Ambiente de demonstração</p><div className="demo-grid">{roles.map(r=><button key={r.role} className="role-button" onClick={()=>loginDemo(r.role)}><ShieldCheck size={18}/><span>{r.label}</span></button>)}</div></>:<form onSubmit={async e=>{e.preventDefault();setBusy(true);setError('');try{await loginWithPassword(email,password)}catch(err){setError(err instanceof Error?err.message:'Falha ao entrar')}finally{setBusy(false)}}}><label>E-mail<input type="email" value={email} onChange={e=>setEmail(e.target.value)} autoComplete="username" required/></label><label>Senha<div className="password-wrap"><input type={show?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password" required/><button type="button" onClick={()=>setShow(v=>!v)}>{show?<EyeOff/>:<Eye/>}</button></div></label>{error&&<p className="form-error">{error}</p>}<button className="primary-button wide" disabled={busy}>{busy?'Entrando...':'Entrar'}</button></form>}<footer>Cronos • Prime Tech</footer></section></main>}
